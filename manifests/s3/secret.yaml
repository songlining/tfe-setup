apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: s3
type: Opaque
stringData:
  # Root credentials for MinIO
  # IMPORTANT: These are for lab use only - use strong credentials in production
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "minioadmin123"
